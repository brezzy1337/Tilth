# Supply-chain guards (see CLAUDE.md → Dependency safety).
# Lifecycle/install scripts are disabled by default; packages that genuinely need a
# build go through pnpm's allowBuilds allowlist with human sign-off.
ignore-scripts=true
